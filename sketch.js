var rose=0;

//The setup function only happens once
function setup() {
	createCanvas(600, 500); //create a 500px X 500px canvas
background(17, 17, 17  );
}

//The draw function happens over and over again
function draw() {
   //an RGB color for the canvas' background
  //circle
 stroke (255, 255, 255)
 fill(255, 255, 255)
textSize(10)
textFont("Allura",40,40)
textStyle(ITALIC)
text("I'm Sorry I was such a bad sister",50,300)
}

